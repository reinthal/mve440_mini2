ensamble <- function(data, method, nrClassifiers){
  # split the data
  
  # train the classifier on each of the splits
  
  # count the votes
  
  # create predictions for the validation set
  
  # return the classifier
  
}